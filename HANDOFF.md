# Session hand-off (2026-06-16)

## TL;DR — current live state (read this first)
- **App is LIVE & fully working** at **https://job-hunt-copilot.fly.dev/** behind Google sign-in (allowlist `nair.sabarish97` + `sabarish.n26`). Open on phone anytime.
- **Feed = ~4,255 jobs LIVE on prod** (100 from light sources + **4,155 HR jobs from the open-jobs dataset**, all 16 ATS incl. Workday). open-jobs rows are heuristic-scored (free); the original feed is Gemini-scored. Master resume present. See "open-jobs dataset integration" below.
- **Gemini paid tier is ON.** The `AQ.` key's free tier was capped at 20 req/day; we **enabled billing + added prepaid credits** on project `gen-lang-client-0163899150`. Prepaid balance = a TRUE hard cap; plus a **$5/mo Cloud budget** ("Gemini $5 cap", alerts 50/90/100%). `LLM_PROVIDER=gemini`, model `gemini-2.5-flash`, in `.env.local` + Fly secrets.
- **Daily cron** (Fly supercronic): refresh-feed 15:00 UTC, poll-gmail + auto-tailor every 30 min. Enrichment uses paid Gemini, falls back to free heuristic on any error.
- **Only open item:** custom domain (Student Pack) — optional, user action. Everything else done.

## open-jobs dataset integration — built (2026-06-18)
Combined the app with **[elliottdehn/open-jobs](https://github.com/elliottdehn/open-jobs)** (CC0, 963,717 jobs from 16 ATS incl. Workday/Ashby/SmartRecruiters — companies our Greenhouse/Lever lists can't see: Amazon, Boeing, Nordstrom, T-Mobile, Providence). Each row ships ~34 pre-extracted LLM fields + embeddings. **Zero recurring cost** — pure-Python streaming filter + the existing free heuristic scorer; no OpenAI/Gemini/Claude calls in the import path.
- **`scripts/import-open-jobs.py`** — streams the 21 GB parquet from `OPEN_JOBS_PARQUET_URL` (or `--parquet`) via `fsspec.open(url).open()` range reads (no full download; embeddings columns are skipped via column projection). Hull filter: `function=hr`, US/US-remote, HR title/alt-title terms, drops staffing. Maps open-jobs fields → `jobs` table incl. **pre-filled** sponsorship_tag (from `visa_sponsorship`), seniority_tag (from `level`), min_years, salary_range, + new columns. Dedups by `open_jobs_id`/`url`/(company,title). Expires rows that fell off the parquet (but never ones saved to the tracker). Flags: `--dry-run`, `--max-batches N` (quick test), `--function`, `--country`, `--title-terms`.
- **Python env:** dedicated venv at **`.venv-openjobs`** (Python 3.12; system Python is 3.14 which has no pyarrow wheels yet). `pip install pyarrow fsspec aiohttp`. Gitignored. The TS wrapper auto-detects it.
- **`scripts/refresh-open-jobs.ts`** (`npm run refresh-open-jobs`) — shells to the Python importer (prefers `.venv-openjobs/bin/python`), then runs `enrichJobHeuristic` (free, no API) on every new `fit_score IS NULL` open-jobs row. If `OPEN_JOBS_PUSH_TO_FLY=1`, snapshots the DB and ships it to Fly (sqlite3 `.backup` → `fly ssh sftp put` → rm wal/shm → machine restart). Default OFF.
- **Schema** (`src/lib/db.ts` migration): added `open_jobs_id, skills_json, company_summary, industry, job_level, job_function, embed_score` to `jobs` + a partial index on `open_jobs_id`. Auto-runs on next `getDb()`.
- **Feed** (`/api/feed` + `/feed`): surfaces `industry` badge, `company_summary` ⓘ tooltip, `skills_json` chips, and a precise `job_level` (open-jobs enum) shown in place of the coarse seniority tag. New `?jobLevel=` / `?jobFunction=` filters + a Level dropdown in the filter bar.
- **Semantic re-rank (optional, near-free):** `scripts/embed-rerank.py` (`npm run embed-rerank`) embeds the master resume ONCE with `text-embedding-3-small` (~$0.0001, needs `OPENAI_API_KEY`) and cosine-scores it vs each open-jobs row's pre-computed `jd_embedding` → `jobs.embed_score`, shown as an "emb NN" badge in the feed. Skips cleanly without a key (embed_score stays NULL; heuristic fit_score unaffected). ⚠️ fetches the heavy `jd_embedding` column from the parquet (most of the 21 GB over HTTP) — run against a LOCAL parquet (`python3 scripts/download.py` once). No OpenAI key in `.env.local` today, so this is wired but inactive.
- **auto-tailor** (`scripts/auto-tailor.ts`): dual threshold — non-open-jobs rows still fire at `AUTO_TAILOR_MIN_FIT` (70); open-jobs rows use `AUTO_TAILOR_MIN_FIT_OPEN_JOBS` (82) so the larger pool doesn't over-tailor.
- **Scheduling: WEEKLY (Sunday 6 AM), not daily.** `npm run install-schedule -- refresh-open-jobs` generates a weekly launchd plist (with `OPEN_JOBS_PUSH_TO_FLY=1` + `~/.fly/bin` on PATH). Runs LOCALLY only — Fly has no pyarrow/parquet and a 1 GB volume. The dataset is a daily-overwritten SNAPSHOT with no delta API, so a bulk pull = re-downloading ~20 GB; daily would be wasteful. The light sources (Greenhouse/Lever/JSearch/LinkedIn) in the existing daily `refresh-feed` cron keep the feed fresh day to day; open-jobs is the weekly bulk refill. (install-schedule.ts now supports interval/daily/weekly jobs; fixed a latent NaN-hour bug in the daily-named-job path.)
- **Download-first model:** scanning the 21 GB parquet over HTTP is slow AND drops (HTTP/2 stream errors, fsspec timeouts ~45 min in). So we DOWNLOAD it once to a local `open-jobs.parquet` (gitignored) and scan that (~seconds). `scripts/download-open-jobs.sh` does a resumable, self-healing HTTP/1.1 download (forces HTTP/1.1 — HTTP/2 was the culprit; auto-resumes via `curl -C -`). `refresh-open-jobs.ts` runs it first when `OPEN_JOBS_DOWNLOAD_URL` is set, then imports from the local `OPEN_JOBS_PARQUET_URL`. The importer now commits per-batch so an interrupted scan keeps its progress.
- **LIVE ON PROD (2026-06-19):** full import of all 963,717 rows → **4,155 HR jobs** across all 16 ATS incl. **workday/ashby/smartrecruiters/dayforce/etc.** Field coverage: job_level/job_function/skills/sponsorship/min_years 100%, industry/company 90%, jd_text 99%, salary ~49%. Heuristic-enriched (free, no LLM): **avg fit 65, 91% ≥60**, top fit 93. Deployed via: (1) `fly deploy --remote-only` (code: migration + feed UI/API), (2) merge open-jobs rows into a pulled copy of the prod DB (`scripts/merge-open-jobs.py`, preserves prod's applications + email_triage_log), (3) snapshot→`fly ssh sftp put` to /app/data/import.db→`mv` over app.db + rm wal/shm→restart machine `d8d2e5ec277048`. **Prod verified:** total jobs 4,255 (100 original + 4,155 open-jobs, all scored), 12 triage logs preserved, master resume preserved, 16 ATS, health ok.
- **Deploy gotchas (learned):** (a) prod container has **no sqlite3 CLI** — merge locally on a pulled copy, push the file back. (b) `fly machine restart` needs the **machine ID** explicitly when non-interactive (`d8d2e5ec277048`). (c) ALWAYS re-pull prod and MERGE (never overwrite with local) — prod crons add email_triage_log rows between pushes (saw 11→12). (d) The 20 GB parquet must be **downloaded locally** then scanned (local scan = **15 s** for all 963K rows vs ~45 min over HTTP that also drops via HTTP/2 stream errors + daily in-place file swaps mid-download). User downloaded it manually to `~/Downloads/open-jobs.parquet` after curl fought the daily swap.
- **Plan file:** `~/.claude/plans/list-all-features-mine-hidden-sutton.md`.

## Phase 2 — auto-refresh + rescore + auto-tailor-on-save (built 2026-06-19/20)
Plan: `~/.claude/plans/list-all-features-mine-hidden-sutton.md` (Phase 2 section).
- **Feature A — rescore feed on master-resume change.** `rescoreAllJobs(db, resume)` (`src/lib/jobs/rescore.ts`) re-runs the FREE heuristic over every job (instant, $0, no API). Auto-fires in `POST /api/resume` after a master swap (returns `rescored: N`); manual `POST /api/feed/rescore` + "Re-score feed" button on Setup; CLI `npm run rescore`. Verified: re-scored 4,305 jobs locally.
- **Feature C — auto-tailor on save (no manual Lab clicks).** `POST /api/applications` and `POST /api/jobs` fire `autoTailorAndPrep(db, appId, getBackgroundProvider())` **fire-and-forget** (`src/lib/auto-pipeline.ts` — Diagnose→Keywords→Rewrite→Outreach→Interview-prep). Gated on `provider.hasKey()`; uses free Gemini background tier. Works because prod is `next start` (unawaited promise survives the response). The 30-min `auto-tailor.ts` poller stays as a safety net.
- **Feature B — GitHub Actions smart refresh** (`.github/workflows/refresh-open-jobs.yml`). Hourly cron (~6 AM–midnight PT) + manual `workflow_dispatch` (with a `force` input). **Version gate:** HEADs the parquet's `Last-Modified` → `actions/cache` key; cache HIT = already imported = ~30 s no-op; MISS = download → import-into-pulled-prod-DB → enrich → push → restart. Ephemeral runner = 20 GB never on the Mac, auto-deleted. Reuses `refresh-open-jobs.ts` (now CI-safe: `pushToFly()` resolves the machine ID + temp-swaps `import.db`→`app.db`). Filter is env-config: `OPEN_JOBS_FUNCTION`/`OPEN_JOBS_TITLE_TERMS`/`OPEN_JOBS_COUNTRY` (HR defaults).
- **ONE-TIME SETUP (user):** add repo secret **`FLY_API_TOKEN`** = `fly tokens create deploy -x 8760h`. Then the workflow self-runs. The Phase-1 Sunday launchd plist is now redundant (Actions replaces it).
- **Changing HR→other:** set `OPEN_JOBS_FUNCTION=engineering` + `OPEN_JOBS_TITLE_TERMS="software engineer,backend,…"` in the workflow env (or `.env.local`); narrow displayed location via `FEED_LOCATION_FILTER`. Pair with an engineering master resume (the heuristic scores off resume experience titles) + run rescore.

## Where we are
Building **Job Hunt Copilot** at `~/Downloads/Claude/Projects/job-hunt-copilot` (Next.js 16 / React 19 / TS / Tailwind, `better-sqlite3` at `data/app.db`). All 7 original phases are built. Recent work: resume-template polish, email triage core, the **Gmail connector**, **Gmail send for outreach**, a shared **writing style guide**, the **auto-tailor / follow-up / interview-prep automation suite**, and (end of this session) **geographic location filtering** for the job feed + first real launch with clean data.

## LIVE on Fly + Gemini paid enrichment — DONE (2026-06-16)
**App is live at https://job-hunt-copilot.fly.dev/** behind Google sign-in (allowlist: nair.sabarish97 + sabarish.n26). Single Fly machine `d8d2e5ec277048` in **sjc**, 1GB encrypted volume `data` holding `app.db`. Full prod sign-in verified via browser (+ negative test: non-allowlisted account → Access Denied). flyctl in `~/.fly` (on PATH via `.zshrc`).
- **Gemini billing saga (resolved).** The user's `GOOGLE_API_KEY` (`AQ.Ab8…JsjQ`, non-standard prefix) is a known Google bug: `AQ.` keys have free-tier quota locked near-0 (gemini-2.5-flash = 20/day, 2.0-flash = 0, lites 404). Tried creating a fresh AI Studio project (blocked by Google anti-abuse "suspicious request"). **Fix: enabled billing** — linked existing "My Billing Account" to project `gen-lang-client-0163899150` (where the key lives, under nair.sabarish97) via the Chrome extension, and user **added prepaid credits** (this project uses Gemini's PREPAY model → balance is a hard cap). Created a **$5/mo Cloud budget** "Gemini $5 cap" scoped to that project, alerts 50/90/100%. Paid tier verified working; **full 85-job rescore succeeded** (21 distinct scores, 0 fallbacks), local + prod re-seeded identically.
- **Cost:** ~$0.0014/enrichment. Realistic ~$0.40/mo; worst case (full daily rescore) ~$2/mo. Prepaid balance can't be exceeded.
- **`enrichJob` retries** LLM 429/503 3× w/ backoff before heuristic fallback; **`enrich-missing.ts`** has `ENRICH_DELAY_MS` throttle (not needed on paid tier; use for free-tier batches). Both deployed to prod.
- **Apify wired** (`misceres/indeed-scraper`, HR/Seattle) in `APIFY_ACTORS`; gated on `APIFY_TOKEN` (unset = off, $0). genericApifyMap extended for Indeed fields. Apify is NOT free for daily use ($5/mo credits) — documented.
- **$0 refresh:** see BACKLOG "$0 job-feed refresh". Sources all free except Apify; enrichment free via Gemini (or heuristic fallback); scheduling free via Fly supercronic. Net: the whole refresh runs at $0.
- **Common ops:** redeploy after code change = `fly deploy --app job-hunt-copilot --remote-only`; re-seed DB = snapshot (`sqlite3 … .backup`) → `fly ssh sftp put` to `/app/data/import.db` → ssh `mv` over `app.db` + rm `-wal`/`-shm` → `fly machine restart`.
- **Remaining (optional, user action):** custom domain from Student Pack → `fly certs add` + DNS + add `https://<domain>/api/auth/callback/google` redirect URI.

## Hosting as a real website (Vercel/Firebase rejected; Fly + keep SQLite) — Batch A DONE
Goal: a real public URL, phone-accessible anywhere, not Tailscale. Decided against serverless (Vercel/Firebase) because `better-sqlite3` is synchronous/local-file across 48 query sites — serverless would force a hosted-DB migration + async refactor. Instead: **persistent container on Fly.io, keep SQLite unchanged**, funded by the user's GitHub Student Pack credits + free domain. Plan file: `~/.claude/plans/so-walk-me-through-mighty-charm.md`. Three batches: A (code/local, done), B (first Fly deploy), C (cron + data + domain).

**Batch A — Google sign-in + deploy files (DONE, verified local):**
- **Auth.js v5** (`next-auth@^5.0.0-beta.31`). `src/auth.ts` (Google provider, JWT sessions so **DB untouched**, `trustHost: true` for self-hosting, `signIn` callback allows only `ALLOWED_EMAIL`, `authorized` callback for the guard). Route `src/app/api/auth/[...nextauth]/route.ts`. Sign-in page `src/app/signin/page.tsx` (server-action `signIn("google")`).
- **Route guard**: this Next 16 variant **renamed `middleware.ts` → `proxy.ts`** (function `proxy`, defaults to Node runtime). Guard at **`src/proxy.ts`** (must be in `src/`, sibling of `app/`, not project root) — `export { auth as proxy }` + matcher protecting everything except `/api/auth`, `/api/cron`, `/signin`, static. Verified: unauthenticated `/`,`/feed`,`/api/jobs` → 307 → `/signin`; `/signin` → 200.
- **Deploy files**: `Dockerfile` (multi-stage, builds better-sqlite3 native, installs **supercronic** for in-container cron, copies whole app so tsx cron scripts have their TS source), `.dockerignore`, `deploy/crontab` (refresh daily 15:00 UTC, poll-gmail + auto-tailor every 30 min — mirrors launchd), `deploy/entrypoint.sh` (supercronic background + `npm run serve` foreground, shared `/app/data` volume), `fly.toml` (single machine, `[[mounts]]` data→/app/data, region sea, no auto-stop so cron fires).
- **Multi-account allowlist**: `ALLOWED_EMAILS` (comma-separated) allows BOTH `nair.sabarish97@gmail.com` + `sabarish.n26@gmail.com` (user wanted both). `AUTH_SECRET` generated in `.env.local`. Also added a **sign-out button** (`src/app/layout.tsx`, server-action `signOut`) and an unauthenticated **`/api/health`** endpoint (excluded from the proxy matcher) for Fly health checks.
- **Build/tsc/eslint clean**; launchd server restarted; verified live: protected→307→/signin, /signin→200, /api/health→200 `{"status":"ok"}`.
- **gcloud can't create the OAuth client**: confirmed via CLI — `iap oauth-clients` is IAP-only, requires an "internal only" brand (no good for consumer Gmail sign-in), can't set a custom redirect URI, AND the IAP OAuth Admin API was shut down Mar 19 2026 (past today). So the Web OAuth client is a Console-only manual step. User chose to keep Google sign-in and do the Console step themselves — **all remaining manual to-dos are written in `BACKLOG.md`** ("YOUR MANUAL TO-DOS"): (1) create the Web OAuth client + fill `AUTH_GOOGLE_ID`/`SECRET`, (2) Fly account + deploy (Batch B), (3) data copy + domain (Batch C). Until (1) is done, `/api/auth/providers` returns 500 but the guard/redirect already work.

## Always-on web server + remote access — DONE (local) / handoff (phone)
Goal: access the app anytime, including from the phone.
- **Always-on production server (DONE, running now)**: `scripts/install-server.ts` (`npm run install-server`) generates `com.jobhuntcopilot.server.plist` — a launchd daemon running `npm run serve` (`next start -H 0.0.0.0 -p 3000`) with **RunAtLoad + KeepAlive** (starts on login, restarts on crash). Installed and loaded this session: the old manual `npm run dev` (PID was 41040) was stopped and replaced; `launchctl list | grep jobhuntcopilot` shows `com.jobhuntcopilot.server` running. Verified `localhost:3000/feed` and LAN `192.168.0.110:3000/feed` both return 200. Binds all interfaces (0.0.0.0) so LAN/Tailscale reach it.
  - **New npm scripts**: `serve` (`next start -H 0.0.0.0 -p 3000`) and `install-server`. Logs to `data/server.log`.
  - **Production, not dev**: no hot reload. After any code change, run `npm run build` then restart the service (`launchctl kickstart -k gui/$(id -u)/com.jobhuntcopilot.server`, or unload/load).
- **LAN access (works now)**: phone on the same WiFi → `http://192.168.0.110:3000`. No tunnel needed. (App has no auth, but on the home LAN that's low-risk.)
- **Anywhere access via Tailscale (USER ACTION — not done)**: the app has no login, so we use Tailscale (private device mesh) rather than a public tunnel — only the user's own logged-in devices can reach it. Steps: install Tailscale on the Mac (App Store or tailscale.com/download), `tailscale up`; install Tailscale on the phone, log in with the **same account**; then open `http://<mac-tailscale-ip>:3000` (or MagicDNS hostname) from the phone anywhere. Tailscale install/auth can't be done from the CLI here (needs the user's account login on both devices).

## Skip Anthropic for enrichment — DONE (Gemini option + offline heuristic fallback)
The diagnostic wide-pull burned the Claude credit balance, so enrichment was returning `400 credit balance too low`. Added two ways to run the feed without paid Claude calls:
- **Gemini free tier (config only)**: the provider layer already supports it. Set `LLM_PROVIDER=gemini` + a free `GOOGLE_API_KEY` (aistudio.google.com) — same LLM-quality scoring, no code change. `enrich-missing` and `refresh-feed` route through `getProvider()`.
- **Offline heuristic fallback (`src/lib/jobs/heuristic.ts`)**: `enrichJobHeuristic(job, resume)` scores with zero API calls. **Domain-driven**, not literal-word: it infers the candidate's domain(s) from their *experience titles* (resume = "HR Analyst"/"HR Advisor" → HR domain, weighted by frequency) and scores a job by whether its title sits in the same domain (`DOMAIN_LEXICONS` for hr/eng/sales/marketing/finance/data/product/design/customer/operations). `fitScore = titleDomainScore*70 + skillOverlap*30` (skills gated down 0.3× when the role is outside the candidate's domain, so generic tools like Excel/SQL don't inflate eng roles). Tags via regex (seniority/minYears/sponsorship). Summaries prefixed "Offline estimate:".
- **Wiring**: `enrichJob` (`src/lib/jobs/enrich.ts`) now uses the LLM when a key is set and **falls back to the heuristic on no-key OR any provider error** (rate limit / no credits / bad JSON). DOL known-sponsor override refactored into `applySponsorOverride` and applied to both paths.
- **No-JD jobs now enriched too**: dropped the `jd_text IS NOT NULL` gate in both `refresh.ts` and `enrich-missing.ts` so title-only LinkedIn cards get a domain score (the heuristic needs only the title; the LLM prompt already tolerates "(no description)").
- **Live-verified** against the real feed (Claude key set but credits depleted, so every call exercised the fallback): all 50 jobs scored offline, **top 18 are all HR/People/Talent/Recruiting roles at 70** (Amazon, Nordstrom, CrowdStrike, HUD, Seattle Humane, mongodb, okta), every eng/SRE/product/marketing role at 0, secondary finance/sales at 23–31. `npm run enrich-missing` runs in heuristic mode when no key is set (prints which mode).
- **To restore LLM scoring**: either top up Claude credits, or set `LLM_PROVIDER=gemini` + `GOOGLE_API_KEY`, then `npm run enrich-missing` re-scores everything (it only touches `fit_score IS NULL`, so null them first to force a re-score).

## HR-relevant job sources — DONE
The feed was company-list-only (Greenhouse/Lever pull *all* jobs from a fixed set of named companies), so big WA employers on Workday/iCIMS/Taleo (Amazon, Microsoft, Boeing, Nordstrom, T-Mobile) were invisible and fit scores capped at ~42 against the HR resume.
- **Curated the Greenhouse/Lever lists** in `src/lib/jobs/sources.ts` to verified-WA-office companies with real HR postings: GH = okta, tanium, brex, samsara, mongodb, stripe, thetradedesk, smartsheet; Lever = highspot, rover, outreach. Removed databricks/airbnb/coinbase/figma (no WA HR) and netflix/plaid (empty boards). Each verified live with `curl` against the public JSON API (200 + ≥1 WA HR role).
- **Added two keyword-search sources** (ask "HR jobs in Seattle" instead of "all jobs at company X"):
  - `src/lib/jobs/jsearch.ts` — JSearch via RapidAPI (aggregates LinkedIn/Indeed/Glassdoor/ZipRecruiter), full JD text, free tier 500 req/mo. Runs only when `RAPIDAPI_KEY` is set. Queries in `JSEARCH_QUERIES`.
  - `src/lib/jobs/linkedin.ts` — LinkedIn guest API scraper, no key/account, card-level data only (no JD; enriches on title+company). Toggle `ENABLE_LINKEDIN`. Queries in `LINKEDIN_QUERIES`. Handles 429/302 by returning `[]`.
  - Both wired into `gatherAll()` in `src/lib/jobs/refresh.ts`.
- **`RAPIDAPI_KEY`** added to `.env.local` (real key, user signed up) and documented in `.env.local.example`.
- **Live-verified**: JSearch returns Amazon/Boeing/Starbucks/Providence/Fred Hutch HRBP roles with 1–8k-char JDs; LinkedIn returns Amazon/Nordstrom/CrowdStrike/SpaceX HR roles. After a **clean DB wipe + refresh** (0 applications referenced jobs, safe): 50 jobs, 18 HR-relevant titles, all 15 JSearch+LinkedIn results HR-focused.
- **New `npm run enrich-missing`** (`scripts/enrich-missing.ts`) — backfills fit scores for any job with a JD but no `fit_score`. Needed because `refresh-feed` only enriches jobs it inserts in the same run; rows inserted while the LLM was unavailable stay unscored.
- ⚠️ **BLOCKER: Anthropic credits depleted** mid-session (the diagnostic `FEED_REFRESH_LIMIT=300` pull burned the balance). All enrichment currently returns `400 credit balance too low`, so the 50 clean jobs are inserted but **unscored**. **Next step: top up at console.anthropic.com/settings/billing, then run `npm run enrich-missing`** to score the feed and confirm HR jobs break past the old ~42 ceiling.

## Job feed location filter + first real run — DONE
- **`FEED_LOCATION_FILTER` env var** (pipe-separated `|`) added to `src/lib/jobs/sources.ts` (documented) and applied at call-time in `src/lib/jobs/refresh.ts` so scripts that call `process.loadEnvFile()` late still pick it up.
- Filter logic: strips "Washington D.C." variations before matching so "Washington, D.C." / "Remote - Washington D.C." don't false-positive on WA-state terms; remote jobs go through the same filter (no blanket pass).
- Set in `.env.local`: `FEED_LOCATION_FILTER=Seattle|Bellevue|Redmond|Kirkland|Tacoma|, Washington|Remote - Washington` — catches "City, Washington" patterns, multi-location strings that include WA, and WA-state remote postings.
- **First real data run**: wiped seed data, ran `npm run refresh-feed`, confirmed 50 WA/WA-remote jobs loaded (all 21 distinct location strings contain Seattle, Bellevue, or WA-state remote). Currently sourced from Databricks, Stripe, Airbnb, Coinbase, Figma (Greenhouse), Netflix, Plaid (Lever), RemoteOK, HackerNews — fit scores top out at ~42 because the configured boards skew tech/eng rather than HR. **Next priority: add HR-relevant companies to `src/lib/jobs/sources.ts`.**

## Auto-tailor on save + follow-up reminders + interview-prep auto-trigger — DONE
Three automations on top of the existing pipeline, all using the **background LLM provider** (Gemini free tier when `GOOGLE_API_KEY`/`GEMINI_API_KEY` is set, else falls back to the default provider) so unattended runs don't burn paid Claude credits.

- **Shared core**: `src/lib/auto-pipeline.ts` — extracted the interactive pipeline/outreach logic into reusable functions so both the Lab UI and background jobs share one implementation:
  - `runPipelineStep(db, applicationId, step, provider?)` — runs diagnose/keywords/rewrite/interview, persists conversation + `pipeline_runs` + (for rewrite) a new resume version, flips `saved` → `tailoring`. Throws `PipelineStepError` (carries an HTTP status) for route-level error mapping.
  - `generateOutreachDraft` / `generateFollowupDraft(db, applicationId, daysSinceApplied, provider?)` — outreach and new follow-up-nudge drafts (new `followupPrompt` in `src/lib/outreach.ts`, short/friendly check-in email).
  - `autoTailorApplication(db, applicationId, provider?)` — runs Diagnose → Keywords → Rewrite → Outreach. **Retry-safe**: skips diagnose/keywords if a `pipeline_runs` row for that step already exists, so a retry after a failed rewrite doesn't redo earlier steps or bloat the shared conversation.
  - `triggerInterviewPrep(db, applicationId, provider?)` — runs the interview-prep step once a tailored resume exists and no `pipeline_runs` row for `step='interview'` exists yet; returns `false` (no-op) otherwise, so it's safe to call speculatively.
- **Auto-tailor poller**: `scripts/auto-tailor.ts` (`npm run auto-tailor`) — finds applications with `status IN ('saved','tailoring') AND resume_version_id IS NULL AND fit_score >= AUTO_TAILOR_MIN_FIT` (default 70, env-configurable) and runs `autoTailorApplication` on each. Errors are logged and retried next run. `scripts/install-schedule.ts` supports scheduling it (`npm run install-schedule -- auto-tailor [minutes]`) — **not yet installed**, that's a user action.
- **Follow-up reminders**: `POST /api/applications/[id]/outreach/followup` (new route) computes days since `updated_at` and generates+persists a follow-up draft via `generateFollowupDraft`. Dashboard (`src/app/page.tsx`) has a new violet "Follow-ups" panel listing applications with `status === "applied"` and 10+ days since the last update, each with a "Draft follow-up" button → "Review draft in Lab" once generated.
- **Interview-prep auto-trigger**: wired `triggerInterviewPrep` (best-effort, errors swallowed) into all three places a status can flip to `interview_requested`: `POST /api/email/triage`, `PATCH /api/email/triage/review/[id]` (confirm action), and `scripts/poll-gmail.ts`.
- **`.env.local.example`**: documented `GOOGLE_API_KEY` as also powering background automation regardless of `LLM_PROVIDER`, and added commented `AUTO_TAILOR_MIN_FIT=70`.

### Bugs found + fixed during live verification (all shared code, so they also fix the interactive Lab)
- **Rewrite step JSON truncation**: `src/lib/providers/claude.ts` had `max_tokens: 4096`, too small for the rewriter's full-resume JSON output → "could not be parsed as JSON" errors. Bumped to `8192`.
- **LanguageTool corrupting proper nouns**: `correctGrammar`/`correctResumeGrammar` (`src/lib/language-tool.ts`) now take a `protectedTerms` list (candidate name, company, job title, school names) and skip any LT match whose original text is a protected word — LT was "auto-correcting" e.g. "Sabarish" → "Safaris" and "Brightwave" → "Bright wave" in outreach/follow-up drafts and resume bullets.

### Live-verified end-to-end
Ran `npm run auto-tailor` twice against the real `data/app.db`. Both eligible applications (Vertex Health HR Business Partner, fit 86; Cascade Director of People & Culture, fit 72) were fully tailored — `resume_version_id` set, `outreach_draft` populated, both now show in the "Tailoring" column on the dashboard. Also live-tested the new follow-up endpoint and confirmed the LanguageTool fix (Brightwave / Sabarish Nair render correctly in the generated draft).

## Writing style guide — DONE
`src/lib/style-guide.ts` is the single source of truth for resume/outreach tone, used by both the XYZ Rewriter prompt and the outreach email prompt:
- `BANNED_PHRASES` — ~35 AI-cliché words/phrases ("synergy", "leverage", "I am writing to express my interest", "in conclusion", etc.)
- `STYLE_RULES` — plain American English, active voice, no exclamation points, **no em dashes ever** (en dashes are fine — user confirmed), avoid formulaic transitions/rule-of-three/robotic symmetry, contractions OK.
- `sanitizeDeep()` — mechanical backstop that strips any em dash (`—` or `--`) from every string in the generated JSON, regardless of prompt compliance.
- `reviewWritingStyle()` — second LLM pass that re-reads the generated tailored resume / outreach draft against `STYLE_RULES` and fixes violations (facts/structure preserved), falling back to the original on failure.
- `src/lib/language-tool.ts` — third pass, grammar-only: calls LanguageTool's public API and auto-applies high-confidence typo/grammar/punctuation/casing fixes. `correctResumeGrammar()` checks only the resume's `summary` + bullet arrays (never contact info, names, dates, company/school names); `correctGrammar()` checks only the outreach email `body` (never the subject). Nothing else is ever sent to LanguageTool.
- Pipeline order in both `pipeline/route.ts` (rewrite step) and `outreach/route.ts`: `reviewWritingStyle()` → LanguageTool grammar pass → `sanitizeDeep()` (final em-dash backstop).
- Verified end-to-end on a real application: regenerated outreach draft had no em dashes, natural contractions, no banned phrases. LanguageTool pass verified live against the real API (typos, apostrophe misuse, sentence-start casing all auto-fixed; company/contact fields untouched).

## Current task: Gmail connector — DONE, OAuth connected and verified live
`.env.local` has real `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` / `GOOGLE_REFRESH_TOKEN`. `npm run poll-gmail` was run against the real inbox: found 6 messages (last 7 days), all correctly classified as `other` (Google account-setup emails, no tracker-relevant content yet), logged to `email_triage_log`, none auto-applied.

✅ App published in Google Cloud Console (confirmed by user — hit Publish on the OAuth consent screen, not just left in Testing) — refresh token is long-lived, no 7-day expiry to worry about.

✅ GitHub account `thakkalisuresh` confirmed by user to be their own account — the private repo push is fine.

`scripts/install-schedule.ts` now supports both jobs: `npm run install-schedule -- poll-gmail [minutes]` (default 30 min, uses `StartInterval`) and `npm run install-schedule [hour] [minute]` (refresh-feed, daily, unchanged). Generated `scripts/com.jobhuntcopilot.pollgmail.plist` (every 30 min) and `scripts/com.jobhuntcopilot.refresh.plist`. **Not yet installed** — the user still needs to run the printed `cp` + `launchctl load` commands for each plist (the script intentionally never calls launchctl itself).

Built this session (verified: `npm run build` = 0, eslint clean, scripts run via `node --import tsx`):
- `scripts/connect-gmail.ts` — one-time local OAuth handshake (loopback redirect on `http://127.0.0.1:53682`). Opens a browser, asks for `gmail.readonly` consent, prints a refresh token. Run via `npm run connect-gmail`.
- `src/lib/gmail.ts` — read-only Gmail client (`googleapis`). `listMessageIds(query)`, `getMessage(id)` (parses From/Subject/Date + decodes plain-text or HTML-stripped body), `hasGmailCredentials()`.
- `scripts/poll-gmail.ts` (`npm run poll-gmail`) — pulls messages from the last 7 days, runs each new one through the existing `email-triage` logic, auto-applies the tracker status when confident, and logs every result (applied or not) to a new `email_triage_log` table for provenance / future review-queue UI. Dedupes by Gmail message id so re-runs are cheap and safe.
- DB: added `email_triage_log` table to `src/lib/db.ts` (gmail_message_id, classification, match, suggested_status, applied flag).
- `.env.local` / `.env.local.example`: added `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_REFRESH_TOKEN`.

**Hard rule honored:** OAuth only, never asks for/accepts the Gmail password. Scope = `gmail.readonly`. Tokens live only in `.env.local`.

## Review queue UI — DONE
Added a "Needs review" panel to the dashboard (`src/app/page.tsx`):
- `GET /api/email/triage/review` (`src/app/api/email/triage/review/route.ts`) — lists `email_triage_log` rows where `applied=0 AND dismissed=0 AND suggested_status IS NOT NULL`, joined to the matched application's company/title.
- `PATCH /api/email/triage/review/[id]` (`src/app/api/email/triage/review/[id]/route.ts`) — body `{ action: "confirm" | "dismiss" }`. `confirm` applies the suggested status to the matched application + sets `applied=1`; `dismiss` sets `dismissed=1` (no tracker change).
- DB: added `dismissed INTEGER NOT NULL DEFAULT 0` to `email_triage_log` (migration + schema).
- Verified end-to-end via browser preview: seeded two fake triage rows, confirmed one (moved a card between columns and removed it from the queue) and dismissed the other (queue emptied, panel disappears). Test rows cleaned up afterward and the real application's status was restored.
- Note: **the dev server actually works via `preview_start`** in this environment (contrary to the old note below) — `.claude/launch.json` now exists with a `job-hunt-copilot` config on port 3000.

## Email-triage matching — role title weighted up
`matchApplication` in `src/lib/email-triage.ts` now scores a role-title match in the email body as +2 (was +1), same as a company-name match. Rationale: ATS senders (Greenhouse, Workday, Lever) rarely match the company's domain, but their templates almost always restate both the company and the role — so company+title in body now scores 4 (confidently auto-applies), while a title-only match scores 2 (stays in the review queue, avoiding ambiguity across same-titled roles at different companies). Verified with a scenario script; pushed as `18f7ff4`.

## Browser extension — Gmail triage + outreach pre-fill — DONE
`extension/` (v0.2.0): "Triage this email" reads the open Gmail message's sender/subject/body (DOM scrape via `activeTab`/`scripting`, no new permissions) and posts to `POST /api/email/triage` with `apply: true` — same classifier as the poller, auto-applies when confident, else shows up in "Needs review". "Fill compose / DM here" writes a saved outreach draft's subject/body into an open Gmail compose box or LinkedIn message box (`/api/jobs` now also returns `recruiter_email`). Never sends/submits. `npm run build` + `npm run lint` clean.

### What's left — next backlog items
None currently queued — all BACKLOG items shipped. Open question: confirm OAuth consent screen is "In production" (✅ confirmed by user 2026-06-15 via Cloud Console — "back to testing" button shown, meaning it's currently published).

## Launchd schedules — ACTIVE
Both `com.jobhuntcopilot.pollgmail` (every 30 min) and `com.jobhuntcopilot.refresh` (daily 08:00) are loaded and running (`launchctl list | grep jobhuntcopilot`). poll-gmail has already completed multiple clean runs; refresh-feed completed its first run (50 jobs added, 47/50 enriched — 3 hit a transient Anthropic 429 rate limit, harmless, will retry next run). The "Load failed: 5: Input/output error" message from `launchctl load` on modern macOS is cosmetic — the job loads fine despite it.

## Remaining backlog (see `BACKLOG.md`)
Browser-extension Gmail/LinkedIn feed → Gmail send (per-message confirm). LinkedIn = pre-fill + user-clicks-send only (automation violates ToS).

## Environment quirks (important)
- **Node via nvm**: prefix Bash with `source ~/.zshrc`. No Homebrew/system node.
- **`source ~/.zshrc` itself exits 126** (a harmless zsh `compdef` warning) — chain subsequent commands with `;`, not `&&`, or they'll be skipped.
- **Dev server is broken in this environment** (Turbopack spawns a node subprocess that can't find node). **`npm run build` works** — use it to verify Next routes/pages. Verify libs via `node --import tsx <script>` (redirect output to a file; piping to `tail` can give a spurious exit 126; run WITHOUT `dangerouslyDisableSandbox`).
- **PDF visual checks**: render with system PyMuPDF — `/usr/bin/python3 -c "import fitz; ..."` (matrix 2.2). DOCX preview via `qlmanage -t -s 1600 -o /tmp <file>`.
- **AGENTS.md / CLAUDE.md**: read the relevant guide in `node_modules/next/dist/docs/` before writing route/page code (this is a modified Next.js).
- Master resume is seeded via `node --import tsx scripts/seed-my-resume.ts` (regenerates `samples/`). Master = Sabarish Nair (real resume + UW Masters).

## Key decisions to honor
Human-in-the-loop on every outbound send and ambiguous status change. No accounts/passwords handled by the assistant. Read-only Gmail scope first. Auto-when-confident + review queue (now persisted in `email_triage_log`). Resume font = Arial; A4; Experience before Education; both must fit page 1 (warn, don't shrink).
